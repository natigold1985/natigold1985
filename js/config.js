/* ============================================================
   Judith Gold — הגדרות אינטגרציה (תשתית מוכנה לחיבור עתידי)
   קובץ אחד מרוכז לכל מפתח/שירות חיצוני שטרם הוגדר.
   האתר עובד גם בלי שממלאים כאן כלום (הכל אופציונלי + fallback
   לוואטסאפ ידני מול יהודית), אבל ברגע שיש ספק סליקה/מייל —
   כל מה שצריך זה למלא את השדות הרלוונטיים כאן.
   ============================================================ */
window.JG_CONFIG = {

  /* ---------- סליקה (תשלומים) ----------
     טרם נבחר ספק סליקה. כשיבחר ספק (טרנזילה / קארדקום / גרואו /
     PayPlus וכו') יש למלא כאן את שם הספק ואת פרטי החיבור שהוא
     יספק (Terminal / API Key / Merchant ID), ולחבר את קריאת ה-API
     שלו בפונקציה submitToPaymentGateway() בקובץ js/checkout.js.
     עד אז, ההזמנה נשמרת והלקוחה מועברת להשלמת תשלום מול יהודית
     בוואטסאפ/טלפון — כדי שלא לאבד אף מכירה בזמן ההמתנה לספק. */
  PAYMENT_PROVIDER: null,   // למשל: "tranzila" | "cardcom" | "grow" | "payplus"
  PAYMENT_CONFIG: {
    // terminalName: "",
    // apiKey: "",
    // merchantId: "",
  },

  /* ---------- הודעת מייל אוטומטית ללקוחה שנטשה עגלה ----------
     שירות חינמי מבוסס דפדפן (ללא שרת) לשליחת מייל ישירות
     ללקוחה. יש להירשם ב-emailjs.com (יש חבילת חינם), וליצור
     שירות + תבנית מייל, ואז להדביק כאן את שלושת המזהים.
     כל עוד השדות ריקים — לא נשלח מייל אוטומטי, והליד עדיין
     נשמר ומועבר ליהודית בוואטסאפ/במייל הרגיל שלה. */
  EMAILJS_PUBLIC_KEY: "",
  EMAILJS_SERVICE_ID: "",
  EMAILJS_TEMPLATE_ID_ABANDONED_CART: "",     // תבנית: "שמרנו לך את העגלה"
  EMAILJS_TEMPLATE_ID_ORDER_CONFIRM: "",      // תבנית: "אישור הזמנה התקבלה"
  EMAILJS_TEMPLATE_ID_WELCOME_DISCOUNT: "",   // תבנית: "הנה קוד ה-10% שלך"

  /* ---------- מייל התראה ליהודית (בעלת העסק) ----------
     Web3Forms — שירות חינמי לשליחת טופס ישירות למייל
     judithgold10@gmail.com בלי שרת. הרשמה חינמית ב-web3forms.com,
     והדבקת מפתח הגישה (Access Key) כאן. */
  WEB3FORMS_KEY: "",

  /* ---------- קוד הנחה 10% תמורת הרשמה לדיוור ----------
     מוצג בפופ-אפ שנפתח ברגע שמוסיפים פריט ראשון לעגלה (פעם אחת
     בלבד לכל מבקרת, בכפוף לשתי תיבות הסימון החובה). הקוד מופעל
     אוטומטית על העגלה הנוכחית ברגע שממלאים את הטופס — אין צורך
     להקליד אותו בקופה. */
  DISCOUNT_CODE: "WELCOME10",
  DISCOUNT_PERCENT: 10,

  /* ---------- תזכורת עגלה נטושה אוטומטית (4-5 שעות אחרי) ----------
     הדפדפן של המבקרת לא יכול "לחכות" שעות ואז לשלוח מייל בעצמו —
     היא כבר סגרה את הטאב. הטיימר חייב לרוץ אצל שירות חיצוני. הכי
     פשוט וחינמי: Make.com — יש בו בדיוק שלב "המתנה" (Delay) ועורך
     מיילים יפה מובנה, בלי צורך בשרת או בכתיבת קוד.

     כל ליד עם הסכמה לדיוור (גם מפופ-אפ ה-10% וגם מנטישת עגלה) נשלח
     לכתובת הזו ברגע שהיא ניתנת, וכל הזמנה שהושלמה נשלחת לאותה כתובת
     כדי שהתרחיש ב-Make ידע לדלג על השליחה למי שכבר קנתה בינתיים.

     הגדרה חד-פעמית (כ-10 דקות, בלי קוד):
     1. הרשמה חינמית ב-make.com.
     2. תרחיש (Scenario) חדש → מודול ראשון: Webhooks > Custom webhook.
        זה ייתן כתובת URL — להדביק אותה כאן ב-ABANDONED_CART_WEBHOOK.
     3. מודול שני: Tools > Sleep — להגדיר 4-5 שעות (18000 שניות).
     4. מודול שלישי: Router — לבדוק אם בינתיים הגיע webhook נוסף
        מאותו email עם type=purchased (למשל דרך Data Store קטן
        שנוצר אוטומטית ב-Make ששומר את כתובות המייל שקנו).
     5. מודול רביעי (בענף שלא קנתה): Email > Send an Email — כאן
        אפשר לעצב מייל יפה עם השם, תוכן העגלה (cartText) וקישור
        לחנות (shop_url).
     כל עוד השדה ריק — שום דבר לא נשלח, והאתר ממשיך לעבוד רגיל. */
  ABANDONED_CART_WEBHOOK: "",

  /* ---------- משלוחים ---------- */
  FREE_SHIPPING_FROM: 199,   // ₪ — משלוח חינם מעל הסכום הזה (תואם את שאר האתר)
  SHIPPING_FEE: 25           // ₪ — דמי משלוח כשלא הגיעו לסכום לעיל
};
